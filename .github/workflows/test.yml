on:
  push:
    branches:
      - test
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: node test.js
        env: 
          GH_TOKEN: ${{ github.token }}